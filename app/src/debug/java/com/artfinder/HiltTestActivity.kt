package com.artfinder

import androidx.activity.ComponentActivity
import dagger.hilt.android.AndroidEntryPoint

/**
 * Empty activity for Hilt-based Compose UI tests to avoid conflicts with MainActivity's content.
 */
@AndroidEntryPoint
class HiltTestActivity : ComponentActivity()
